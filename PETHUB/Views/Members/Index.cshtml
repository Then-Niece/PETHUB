@model IEnumerable<PETHUB.Models.ApplicationUser>

@{
    ViewData["Title"] = "Members";
}

<div class="container-fluid px-2 py-4">

    <div class="admin-header">

        <div>
            <h2 class="admin-title">Members</h2>
            <p class="admin-subtitle">
                Manage registered community members.
            </p>
        </div>

        <a asp-action="Create" class="admin-add-btn">
            + New Member
        </a>

    </div>

    <div class="admin-card">

        <div class="admin-card-header">

            <div class="search-box">
                <i data-lucide="search" class="search-icon"></i>

                <input class="admin-search"
                       placeholder="Search members..." />
            </div>

            <span class="member-count">
                @Model.Count() Members
            </span>

        </div>

        

            <table class="admin-table">

                <thead>

                    <tr>

                        <th>Username</th>

                        <th>Full Name</th>

                        <th>Email</th>

                        <th class="text-center">Contact</th>

                        <th class="text-center">Gender</th>

                        <th class="text-center">Role</th>

                        <th class="text-center">Status</th>

                        <th class="text-center" >
                            Actions
                        </th>

                    </tr>

                </thead>

                <tbody>

                    @foreach (var item in Model)
                    {
                        <tr>

                            <td>@item.UserName</td>

                            <td class="full-name-col">
                                @item.FirstName @item.LastName
                            </td>

                            <td class="email-col">
                                @item.Email
                            </td>

                            <td>
                                @item.ContactNumber
                            </td>

                            <td class="text-center">
                                <span class="gender-pill @(item.Gender == "Male" ? "male" : "female")">
                                    @item.Gender
                                </span>
                            </td>

                            <td class="text-center">
                                <span class="role-pill">
                                    @ViewBag.MemberRoles[item.Id]
                                </span>
                            </td>

                            <td class="text-center">
                                <span class="status-pill @(item.Status == UserStatus.Active ? "active" : "inactive")">
                                    @(item.Status == UserStatus.Active ? "Active" : "Inactive")
                                </span>
                            </td>

                            <td class="text-center">

                                <div class="action-dropdown">

                                    <button type="button" class="action-menu-btn">

                                        <i data-lucide="ellipsis-vertical"></i>

                                    </button>

                                    <div class="action-menu">

                                        <a asp-action="Details"
                                           asp-route-id="@item.Id">
                                            View
                                        </a>

                                        <a asp-action="Edit"
                                           asp-route-id="@item.Id">
                                            Edit
                                        </a>

                                        <div class="menu-divider"></div>

                                        <a asp-action="Delete"
                                           asp-route-id="@item.Id"
                                           class="delete-item">
                                            Delete
                                        </a>

                                    </div>

                                </div>

                            </td>

                        </tr>
                    }

                </tbody>

            </table>

            

      

    </div>

    <div class="admin-pagination">

        <div class="pagination-info">
            Showing 1–10 of @Model.Count() members
        </div>

        <div class="pagination-links">

            <a href="#" class="page-btn disabled">
                &lt;
            </a>

            <a href="#" class="page-btn active">
                1
            </a>

            <a href="#" class="page-btn disabled">
                &gt;
            </a>

        </div>

    </div>

</div>