@model IEnumerable<PETHUB.Models.ApplicationUser>

@{
    ViewData["Title"] = "Members";
}

<div class="container mt-4">
    <h2 class="mb-3">Members</h2>
    <p>
        <a asp-action="Create" class="btn btn-primary">Add New Member</a>
    </p>

    <table class="table table-striped table-hover">
        <thead class="table-dark">
            <tr>
                <th>@Html.DisplayNameFor(model => model.First().UserName)</th>
                <th>@Html.DisplayNameFor(model => model.First().FirstName)</th>
                <th>@Html.DisplayNameFor(model => model.First().LastName)</th>
                <th>@Html.DisplayNameFor(model => model.First().Email)</th>
                <th>@Html.DisplayNameFor(model => model.First().ContactNumber)</th>
                <th>@Html.DisplayNameFor(model => model.First().Address)</th>
                <th>@Html.DisplayNameFor(model => model.First().Gender)</th>
                <th>@Html.DisplayNameFor(model => model.First().Birthdate)</th>
                <th>Role</th>
                <th>@Html.DisplayNameFor(model => model.First().Status)</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.UserName</td>
                    <td>@item.FirstName</td>
                    <td>@item.LastName</td>
                    <td>@item.Email</td>
                    <td>@item.ContactNumber</td>
                    <td>@item.Address</td>
                    <td>
                        <span class="badge @(item.Gender == "Male" ? "bg-primary" : "bg-pink")">
                            @item.Gender
                        </span>
                    </td>
                    <td>@item.Birthdate?.ToString("MMM dd, yyyy")</td>
                    <td>
                        <span class="badge bg-info">@ViewBag.MemberRoles[item.Id]</span>
                    </td>
                    <td>
                        <span class="badge @(item.Status == UserStatus.Active ? "bg-success" : "bg-secondary")">
                            @(item.Status == UserStatus.Active ? "Active" : "Inactive")
                        </span>
                    </td>

                    <td>
                        <div class="btn-group" role="group">
                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning btn-sm">Edit</a>
                            <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info btn-sm">Details</a>
                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm">Delete</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
